services:
  - type: web
    name: vortex-lobby
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: node lobby.js
    healthCheckPath: /health
    envVars:
      - key: PORT
        value: "10000"
