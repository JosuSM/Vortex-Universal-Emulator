# Copy this file to keystore.properties for local release signing.
# Do not commit the real keystore.properties or the keystore file.
storeFile=keystore/release-signing.jks
storePassword=replace-with-store-password
keyAlias=vortex-release
keyPassword=replace-with-key-password
